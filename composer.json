{
    "name": "eminiarts/relationship-selector",
    "description": "A Laravel Nova field.",
    "keywords": [
        "laravel",
        "nova"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1.0"
    },
    "autoload": {
        "psr-4": {
            "Eminiarts\\RelationshipSelector\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Eminiarts\\RelationshipSelector\\FieldServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
