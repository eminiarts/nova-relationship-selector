{
    "name": "eminiarts/nova-relationship-selector",
    "description": "A Laravel Nova Package to group Relationships into a Selector.",
    "keywords": [
        "laravel",
        "nova"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1.0"
    },
    "autoload": {
        "psr-4": {
            "Eminiarts\\RelationshipSelector\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Eminiarts\\RelationshipSelector\\FieldServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
